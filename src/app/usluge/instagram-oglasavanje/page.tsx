import type { Metadata } from "next";
import SoonPage from "@/components/service/SoonPage";

export const metadata: Metadata = {
  title: "Instagram oglašavanje",
  description: "Stranica je u pripremi.",
};

export default function InstagramOglasavanjePage() {
  return <SoonPage title="Instagram oglašavanje" />;
}
